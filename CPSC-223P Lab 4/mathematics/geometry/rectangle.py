#A function that gives the permiter of a rectangle
def perimeter(*,length,width):
    return (2*length) + (2*width)
#A function that gives the area of a rectangle
def area(*,length, width):
    return length*width
